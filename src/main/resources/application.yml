spring:
  datasource:
    url: jdbc:mysql://localhost:3306/rsSystem?useUnicode=true&characterEncoding=UTF8
    username: root
    password: root
    driver-class-name: com.mysql.jdbc.Driver
  jpa:
    hibernate:
      ddl-auto: create